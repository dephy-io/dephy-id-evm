{
  "name": "tool-device-simulator",
  "scripts": {
    "build": "rimraf dist/* && tsc -p tsconfig.json",
    "cli": "ts-node ./src/index.ts"
  },
  "devDependencies": {
    "@types/yargs": "^17.0.32",
    "rimraf": "^6.0.0",
    "ts-node": "^10.9.2"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "ethers": "^5.7.2",
    "yargs": "^17.7.2"
  }
}
